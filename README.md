# RetailSense
Building an end-to-end demand forecasting pipeline for retail products, then augmented it with an LLM that reads real-world news and events to automatically inject external signals, improving forecast accuracy during disruption periods.
